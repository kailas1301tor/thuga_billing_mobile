name: print_bluetooth_thermal
description: Web-build stub — no native Bluetooth assets.
version: 1.2.2
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms: {}
